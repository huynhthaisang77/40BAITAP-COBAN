// 38. Tạo một hàm nhận vào một chuỗi và trả về số lượng từ trong chuỗi đó.
const text = 'quy nhon binh dinh'

function check_quantity(x) {
    const counts = 0
    for (let i = 0; i < x.length; i++) {
        if (x[i] == x[i]) {
            
            
        } 
         
        
        
    }
    return counts
}

console.log(check_quantity(text));

